;; template for emacs settings

;; for mac (function is defined in defun.el)
(when (system-type-is-darwin)
  ;; call functions
)
;; for linux (function is defined in defun.el)
(when (system-type-is-gnu)
  ;; call functions
)

;; for gui emacs (non-terminal)
(when (display-graphic-p)
  ;; call functions
)
